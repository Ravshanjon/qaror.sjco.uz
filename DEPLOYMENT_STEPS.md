# Пошаговый деплой на продакшен (qaror.sjco.uz)

## ⚠️ ВАЖНО: Перед началом

**Создайте бэкап:**
```bash
cd /var/www/qaror.sjco.uz
mysqldump -u root -p qaror_db_dev > backup_$(date +%Y%m%d_%H%M%S).sql
cp .env .env.backup
```

---

## 📋 ФАЗА 1: Критические исправления безопасности

### Шаг 1.1: Получить код
```bash
cd /var/www/qaror.sjco.uz
git pull origin main
```

**Ожидаемый результат:** Должно загрузиться ~17 измененных файлов

---

### Шаг 1.2: Запустить миграции
```bash
# Проверить миграции (безопасно)
php artisan migrate --pretend

# Если всё ок - запустить
php artisan migrate --force
```

**Ожидаемые миграции:**
- `add_is_admin_to_users_table` - добавляет поле is_admin
- `add_indexes_to_qarors_table` - добавляет индексы

**Ожидаемый результат:** `Migrated: ...` (2 миграции)

---

### Шаг 1.3: Установить is_admin для существующих админов
```bash
php artisan tinker
```

В консоли Tinker:
```php
$users = App\Models\User::all();
foreach($users as $user) {
    $user->is_admin = true;
    $user->save();
    echo "User {$user->email} is now admin\n";
}
exit
```

**Ожидаемый результат:** Сообщения о том, что пользователи стали админами

---

### Шаг 1.4: Обновить .env (критические настройки)
```bash
nano .env
```

**Измените следующие строки:**
```env
APP_DEBUG=false
APP_ENV=production
SESSION_ENCRYPT=true
```

**Сохраните:** `Ctrl+O`, `Enter`, `Ctrl+X`

---

### Шаг 1.5: Очистить кеши
```bash
php artisan optimize:clear
php artisan config:cache
php artisan route:cache
php artisan view:cache
```

**Ожидаемый результат:** Сообщения об очистке и кешировании

---

### Шаг 1.6: Перезапустить сервисы
```bash
sudo systemctl restart nginx
sudo systemctl restart php8.2-fpm
```

**Ожидаемый результат:** Без ошибок

---

### ✅ ВЕРИФИКАЦИЯ ФАЗЫ 1

**Проверьте каждый пункт:**

#### 1. Проверить AJAX поиск
```bash
curl "https://qaror.sjco.uz/qarorlar/ajax-search?q=test"
```
**Ожидаемый результат:** JSON массив с результатами (или пустой [])

#### 2. Проверить авторизацию админки
- Откройте: https://qaror.sjco.uz/admin
- Войдите с учетными данными админа
- **Должно:** Успешный вход

#### 3. Проверить пагинацию
- Откройте: https://qaror.sjco.uz
- **Должно:** Внизу страницы есть пагинация (1, 2, 3...)

#### 4. Проверить что APP_DEBUG=false
- Откройте несуществующую страницу: https://qaror.sjco.uz/nonexistent
- **Должно:** Общее сообщение об ошибке (БЕЗ стектрейса!)

#### 5. Проверить логи
```bash
tail -f storage/logs/laravel.log
```
- **Должно:** Нет критических ошибок
- `Ctrl+C` для выхода

---

### ⏸️ ПАУЗА: Всё работает?

**Если ДА** - переходите к Фазе 2
**Если НЕТ** - смотрите раздел "Откат Фазы 1" в PRODUCTION_FIX.md

---

## 📋 ФАЗА 2: Оптимизации производительности

### Шаг 2.1: Обновить .env для очередей
```bash
nano .env
```

**Добавьте/измените:**
```env
QUEUE_CONNECTION=database
```

**Сохраните:** `Ctrl+O`, `Enter`, `Ctrl+X`

---

### Шаг 2.2: Настроить Supervisor для queue worker
```bash
# Скопировать конфигурацию
sudo cp supervisor-queue.conf /etc/supervisor/conf.d/qaror-queue-worker.conf

# Обновить supervisor
sudo supervisorctl reread
sudo supervisorctl update

# Запустить worker
sudo supervisorctl start qaror-queue-worker:*

# Проверить статус
sudo supervisorctl status
```

**Ожидаемый результат:** `qaror-queue-worker:qaror-queue-worker_00   RUNNING`

---

### Шаг 2.3: Очистить конфигурацию
```bash
php artisan config:clear
php artisan config:cache
```

---

### Шаг 2.4: Перезапустить сервисы
```bash
sudo systemctl restart php8.2-fpm
```

---

### ✅ ВЕРИФИКАЦИЯ ФАЗЫ 2

**Проверьте каждый пункт:**

#### 1. Queue worker работает
```bash
sudo supervisorctl status qaror-queue-worker:*
```
**Ожидаемый результат:** `RUNNING`

#### 2. Проверить логи queue worker
```bash
tail -f /var/www/qaror.sjco.uz/storage/logs/queue-worker.log
```
- **Должно:** Лог запуска worker
- `Ctrl+C` для выхода

#### 3. Протестировать Excel импорт
- Войдите в админку: https://qaror.sjco.uz/admin
- Найдите раздел импорта
- Загрузите тестовый Excel файл
- **Должно:** Сообщение "Import jarayoni boshlandi!"
- Импорт должен завершиться в фоне (проверить в БД через 1-2 минуты)

#### 4. Проверить счетчик просмотров
```bash
# Подключиться к БД
mysql -u root -p qaror_db_dev

# В MySQL:
SELECT id, title, views FROM qarors LIMIT 5;
```
- Откройте любой PDF документ на сайте
- Обновите страницу несколько раз
- Снова проверьте в БД - `views` должен увеличиться
- `exit` для выхода из MySQL

#### 5. Проверить производительность
- Откройте: https://qaror.sjco.uz
- Фильтруйте по годам
- **Должно:** Быстрая загрузка (менее 1 секунды)

---

### ⏸️ ПАУЗА: Всё работает?

**Если ДА** - переходите к Фазе 3
**Если НЕТ** - смотрите раздел "Откат Фазы 2" в PRODUCTION_FIX.md

---

## 📋 ФАЗА 3: Безопасность и качество

### Шаг 3.1: Очистить кеши (Фаза 3 уже в коде)
```bash
php artisan optimize:clear
php artisan config:cache
php artisan route:cache
php artisan view:cache
```

---

### Шаг 3.2: Перезапустить сервисы
```bash
sudo systemctl restart nginx
sudo systemctl restart php8.2-fpm
```

---

### ✅ ВЕРИФИКАЦИЯ ФАЗЫ 3

**Проверьте каждый пункт:**

#### 1. Проверить Security Headers
```bash
curl -I https://qaror.sjco.uz
```

**Должны быть заголовки:**
```
X-Frame-Options: SAMEORIGIN
X-Content-Type-Options: nosniff
X-XSS-Protection: 1; mode=block
Strict-Transport-Security: max-age=31536000; includeSubDomains
Content-Security-Policy: ...
```

**Или в браузере:**
- Откройте: https://qaror.sjco.uz
- Нажмите F12 (DevTools)
- Вкладка Network
- Обновите страницу
- Кликните на первый запрос
- Response Headers → найдите заголовки выше

#### 2. Проверить валидацию Excel импорта
- Создайте Excel файл с невалидными данными (пустой `number` или `title`)
- Загрузите через админку
- Проверьте логи:
```bash
tail -f storage/logs/laravel.log
```
- **Должно:** Сообщение "Excel import row validation failed"
- Валидные строки должны импортироваться, невалидные - пропускаться

#### 3. Проверить что сайт работает нормально
- Откройте: https://qaror.sjco.uz
- Проверьте что стили загружаются
- Проверьте что скрипты работают
- **Если НЕТ** - CSP слишком строгий, смотрите раздел "Откат Фазы 3"

---

## 🎉 ЗАВЕРШЕНО!

Все три фазы успешно задеплоены!

### Финальная проверка:
```bash
# Проверить статус всех сервисов
sudo systemctl status nginx
sudo systemctl status php8.2-fpm
sudo supervisorctl status

# Проверить логи на ошибки
tail -100 storage/logs/laravel.log
tail -100 /var/log/nginx/error.log
```

---

## 🔥 В случае проблем

### Быстрый откат всех изменений

```bash
# Остановить queue worker
sudo supervisorctl stop qaror-queue-worker:*

# Откатить миграции
php artisan migrate:rollback --step=2

# Восстановить .env
cp .env.backup .env

# Очистить кеши
php artisan optimize:clear

# Перезапустить
sudo systemctl restart nginx
sudo systemctl restart php8.2-fpm
```

### Восстановить БД из бэкапа

```bash
# Найти бэкап
ls -lh backup_*.sql

# Восстановить (ОСТОРОЖНО - удалит текущие данные!)
mysql -u root -p qaror_db_dev < backup_YYYYMMDD_HHMMSS.sql
```

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи: `storage/logs/laravel.log`
2. Проверьте логи веб-сервера: `/var/log/nginx/error.log`
3. Проверьте статус сервисов: `sudo systemctl status nginx php8.2-fpm`
4. Смотрите детальные инструкции в `PRODUCTION_FIX.md`
